set(ETL_VERSION @PROJECT_VERSION@)

@PACKAGE_INIT@

include(FindPackageHandleStandardArgs)

set_and_check(ETL_INCLUDE_DIR "@PACKAGE_INCLUDE_INSTALL_DIR@")
set(ETL_LIBRARY rdmppwrapper)
set(ETL_FOUND yes)

check_required_components(Etl)
